# @anoblet/atomic

A shared atomic CSS library for the `@anoblet` ecosystem.

## Installation

```bash
pnpm add @anoblet/atomic
```

## Usage

Import the styles in your Lit components:

```typescript
import { styles } from '@anoblet/atomic';

static styles = [styles, css`...`];
```
